# Task: assemble the MVP spec

Type: task
Status: resolved
Blocked by: 04, 05, 06, 08, 09, 10, 11

## Question

Compile every decision on the map into the build-ready MVP spec — the map's destination deliverable — at `.scratch/isometric-dashboard/spec.md`.

## Answer

Assembled at [`spec.md`](../spec.md). Structure: vision → domain model → what the MVP is/is not → frontend (themes, timeline, chrome) → backend (Convex, sync, key handling, share link) → onboarding & demo → milestones in frontend-first order → open items. Every section links back to its source ticket or ADR rather than restating detail. The open-items section is explicit about what still needs the user: the two prototype verdicts, the Evergrow purchase, the Stripe seed run (ticket 07), and the veto pass over the autonomously made decisions (tickets 04, 08–11).